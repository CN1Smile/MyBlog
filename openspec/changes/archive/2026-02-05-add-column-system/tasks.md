# Tasks

## 1. 数据结构扩展

- [x] 1.1 在文章数据中添加 `column` 字段（专栏 slug）
- [x] 1.2 创建 `data/columns.json` 专栏数据文件结构

## 2. GitHub Storage API 扩展

- [x] 2.1 添加 `getColumns()` 获取所有专栏
- [x] 2.2 添加 `createColumn(column)` 创建专栏
- [x] 2.3 添加 `updateColumn(slug, column)` 更新专栏
- [x] 2.4 添加 `deleteColumn(slug)` 删除专栏
- [x] 2.5 添加 `getPostsByColumn(slug)` 按专栏筛选文章

## 3. 编辑器专栏选择

- [x] 3.1 在 `editor.html` 添加专栏选择/输入组件
- [x] 3.2 加载已有专栏列表作为下拉选项
- [x] 3.3 支持输入新专栏名自动创建
- [x] 3.4 发布文章时保存专栏信息

## 4. 专栏列表页面

- [x] 4.1 创建 `columns.html` 页面结构
- [x] 4.2 创建 `js/columns.js` 页面逻辑
- [x] 4.3 展示所有专栏卡片（名称、描述、文章数）
- [x] 4.4 点击专栏进入该专栏文章列表

## 5. 首页侧边栏专栏

- [ ] 5.1 在首页侧边栏添加专栏列表区域（可选，后续迭代）
- [ ] 5.2 加载并显示专栏列表
- [ ] 5.3 点击专栏筛选显示该专栏文章

## 6. 文章详情页专栏

- [ ] 6.1 在 `post.html` 显示文章所属专栏（可选，后续迭代）
- [ ] 6.2 专栏名可点击跳转到专栏页面

## 7. 导航更新

- [x] 7.1 在导航栏添加「专栏」入口